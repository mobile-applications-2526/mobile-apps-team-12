export type User = {
    firstName : String,
    lastName: String,
    email: String,
    phonenumber: String,
    password: String,
}

export type Pet = {
    id: string,
    name: string,
    birthdate: string,
    description: string,
    created_at: string,
    updated_at: string,
}